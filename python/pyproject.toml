[project]
name = "python-practice"
version = "0.1.0"
requires-python = ">=3.12"

# uv workspace: add new sub-projects under members as you create them, e.g.:
# [tool.uv.workspace]
# members = ["my-project"]

